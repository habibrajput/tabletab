import { FeaturePage, featureMetadata } from "../_components/feature-page";

export const metadata = featureMetadata("localization");

export default function Page() {
  return <FeaturePage slug="localization" />;
}
