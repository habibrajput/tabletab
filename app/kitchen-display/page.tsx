import { FeaturePage, featureMetadata } from "../_components/feature-page";

export const metadata = featureMetadata("kitchen-display");

export default function Page() {
  return <FeaturePage slug="kitchen-display" />;
}
