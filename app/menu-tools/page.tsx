import { FeaturePage, featureMetadata } from "../_components/feature-page";

export const metadata = featureMetadata("menu-tools");

export default function Page() {
  return <FeaturePage slug="menu-tools" />;
}
