import { FeaturePage, featureMetadata } from "../_components/feature-page";

export const metadata = featureMetadata("realtime-sync");

export default function Page() {
  return <FeaturePage slug="realtime-sync" />;
}
