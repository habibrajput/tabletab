import { FeaturePage, featureMetadata } from "../_components/feature-page";

export const metadata = featureMetadata("cash-register");

export default function Page() {
  return <FeaturePage slug="cash-register" />;
}
