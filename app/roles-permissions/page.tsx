import { FeaturePage, featureMetadata } from "../_components/feature-page";

export const metadata = featureMetadata("roles-permissions");

export default function Page() {
  return <FeaturePage slug="roles-permissions" />;
}
