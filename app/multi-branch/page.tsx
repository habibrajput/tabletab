import { FeaturePage, featureMetadata } from "../_components/feature-page";

export const metadata = featureMetadata("multi-branch");

export default function Page() {
  return <FeaturePage slug="multi-branch" />;
}
