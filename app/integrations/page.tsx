import { FeaturePage, featureMetadata } from "../_components/feature-page";

export const metadata = featureMetadata("integrations");

export default function Page() {
  return <FeaturePage slug="integrations" />;
}
