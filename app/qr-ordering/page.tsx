import { FeaturePage, featureMetadata } from "../_components/feature-page";

export const metadata = featureMetadata("qr-ordering");

export default function Page() {
  return <FeaturePage slug="qr-ordering" />;
}
