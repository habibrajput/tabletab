# Copy to .env.production on the VPS (and .env.development for the dev container).
# APP_PORT is the HOST port the container is published on. The container itself
# always listens on 3000. Pick a port that is free on the box — nginx proxies to it.
APP_PORT=3000
